import {requireFinite,radToDeg} from './core.js';
import {fretFraction} from './frets.js';
export function multiscalePlan({trebleScaleMm,bassScaleMm,count,fretCount,perpendicularFret,nutSpreadMm,bridgeSpreadMm}){const treble=requireFinite(trebleScaleMm,'Treble scale',{min:200,max:1200}),bass=requireFinite(bassScaleMm,'Bass scale',{min:200,max:1200}),strings=requireFinite(count,'String count',{min:2,max:12,integer:true}),frets=requireFinite(fretCount,'Fret count',{min:1,max:36,integer:true}),perp=requireFinite(perpendicularFret,'Perpendicular fret',{min:0,max:frets,integer:true}),nutSpread=requireFinite(nutSpreadMm,'Nut string spread',{min:1,max:150}),bridgeSpread=requireFinite(bridgeSpreadMm,'Bridge string spread',{min:1,max:250});if(bass<treble)throw new RangeError('Bass scale must be equal to or longer than treble scale.');const p=fretFraction(perp),perpendicularX=p*treble,stringsData=Array.from({length:strings},(_,i)=>{const t=i/(strings-1),scaleMm=treble+(bass-treble)*t,nutYmm=nutSpread*t,bridgeYmm=bridgeSpread*t,nutXmm=perpendicularX-p*scaleMm,bridgeXmm=nutXmm+scaleMm;return {string:i+1,scaleMm,nutXmm,nutYmm,bridgeXmm,bridgeYmm}}),fretRows=Array.from({length:frets},(_,fi)=>{const fret=fi+1,f=fretFraction(fret);return {fret,points:stringsData.map(s=>({string:s.string,xMm:s.nutXmm+f*s.scaleMm,yMm:s.nutYmm+f*(s.bridgeYmm-s.nutYmm)}))}}),endAngle=(a,b)=>radToDeg(Math.atan2(b.xMm-a.xMm,b.yMm-a.yMm)),nutAngleDeg=endAngle(stringsData[0],stringsData.at(-1)),bridgeAngleDeg=endAngle({xMm:stringsData[0].bridgeXmm,yMm:stringsData[0].bridgeYmm},{xMm:stringsData.at(-1).bridgeXmm,yMm:stringsData.at(-1).bridgeYmm}),perpRow=perp?fretRows[perp-1]:{points:stringsData.map(s=>({xMm:s.nutXmm,yMm:s.nutYmm}))},perpendicularDeviationMm=Math.max(...perpRow.points.map(x=>x.xMm))-Math.min(...perpRow.points.map(x=>x.xMm));return {strings:stringsData,frets:fretRows,perpendicularFret:perp,perpendicularXmm:perpendicularX,perpendicularDeviationMm,nutAngleDeg,bridgeAngleDeg,maxFanAngleDeg:Math.max(Math.abs(nutAngleDeg),Math.abs(bridgeAngleDeg))}}
